version: "3.9"
services:
  postgres:
    image: postgres:16
    container_name: wc-planner-postgres
    environment:
      POSTGRES_DB: 
      POSTGRES_USER: 
      POSTGRES_PASSWORD: 
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: wc-planner-redis
    ports:
      - ""
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
